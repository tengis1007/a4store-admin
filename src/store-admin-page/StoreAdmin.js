import React from 'react'; // Assuming you have a firebase.js file for Firebase config
import Header from './pages/home/header'; // Make sure the Header component path is correct

const AdminStore = () => {
  return (
      <Header />
  );
};

export default AdminStore;
